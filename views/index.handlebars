<div class="jumbotron">
<h1 class="display-4">Bem vindo ao blog</h1>
<p class="lead">Primeiro contato com node e MongoDB , Aproveite :D</p>
<hr class="my-4">
<p>Para acessar mais funções crie sua conta </p>
<a class="btn btn-primary btn-lg" href="/usuarios/registro" role="button">Crie uma conta</a>
</div>

<hr>
<h2>Postagens Recentes</h2>
{{#each postagens}}
<div class="card mt-4">
    <div class="card-body">
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
        <a href="/postagem/{{slug}}"><button class="btn btn-primary">Leia mais</button></a>
        <hr>
        <small>Data de publicação:{{data}}</small><br>
        <small>Categoria: {{categoria.nome}}</small>
    </div>
    
        
</div>
{{else}}
    <h5>Nenhuma Postagem</h5>
{{/each}}
